#include<stdio.h>
#include<conio.h>
int main()
{
    char str[10] = {'B','H','O','P','A','L'}; // or we can simply write "BHOPAL"
    printf("%s",str);
    /* for(int i = 0; str[i] != '\0';i++)
    {
        printf("%c",str[i]);
    } */
    getch();
    return 0;
}